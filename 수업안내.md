# 모델 뷰어 · 코멘트 — 수업 안내 (4주차, 16:00–17:00)

오늘 만드는 것: **내 주소에 내 모델(IFC·DXF)을 올리고, 남이 들어와서 객체를 찍고 코멘트를 남기는 서비스.**
뷰어 코드는 이미 다 만들어져 있습니다. 여러분이 하는 건 계정 세 개를 만들고, 키 두 개를 넣고, AI에게 한 문장씩 시키는 겁니다.

---

## 0. 준비물 확인 (수업 시작 전)

| 있어야 하는 것 | 없으면 |
| --- | --- |
| Claude Code가 켜지는 터미널 | 3주와 같은 환경 |
| 이 폴더 (`viewer-starter`) | 강사가 주는 zip을 풀어 둔다 |
| Node.js | 3주에 rhwp 돌린 그 Node.js |
| 깃허브 계정 | 없으면 **1번**에서 만든다 (2분) |
| Vercel 계정 | 없으면 **2번**에서 깃허브로 로그인 (1분) |
| 수파베이스 계정 | **3번**에서 깃허브로 로그인 (2분) |

세 계정 다 **깃허브 계정 하나로 로그인**하면 제일 빠릅니다. 이메일로 따로 가입하면 인증 메일 기다리는 데서 막힙니다.

---

## 1. 깃허브 — 계정과 로그인

**계정이 없으면** github.com → Sign up → 이메일, 비밀번호, 이름. 인증 코드 메일이 옵니다.

**터미널에서 로그인** (한 번만):

```
gh auth login
```

물어보는 대로 고릅니다. `GitHub.com` → `HTTPS` → `Login with a web browser` → 화면에 뜨는 8자리 코드를 브라우저에 붙여넣기.
이게 되면 AI가 `git push`를 할 때 비밀번호를 안 물어봅니다.

`gh`가 없다고 나오면: 강사에게. (윈도우: `winget install GitHub.cli`)

**토큰을 직접 만들 필요는 없습니다.** `gh auth login`이 대신 만듭니다. 혹시 AI가 "Personal Access Token이 필요하다"고 하면 위 명령을 먼저 했는지 확인하세요.

---

## 2. Vercel — 배포 계정

vercel.com → Sign Up → **Continue with GitHub**. 끝입니다.

터미널 로그인은 AI가 배포할 때 알아서 시킵니다(`vercel login` → 브라우저 확인). 미리 하고 싶으면:

```
npm i -g vercel
vercel login
```

---

## 3. 수파베이스 — 파일과 코멘트가 저장되는 곳

### 3-1. 가입과 프로젝트

1. supabase.com → Start your project → **Continue with GitHub**
2. **New project** → 이름은 아무거나(예: `viewer`), **Database Password는 만들어서 어딘가 적어 둡니다**(오늘은 안 쓰지만 나중에 필요), Region은 `Northeast Asia (Seoul)`
3. 2~3분 기다립니다. 이 사이에 4번을 읽으세요.

### 3-2. 표 만들기 (SQL 붙여넣기)

1. 왼쪽 메뉴 **SQL Editor** → **New query**
2. 이 폴더의 `supabase.sql` 파일을 열어서 **전체 복사** → 붙여넣기 → **Run**
3. 아래에 `Success. No rows returned` 가 나오면 된 겁니다.
4. 확인: 왼쪽 **Table Editor**에 `models`, `comments` 두 표가 보이는지. **Storage**에 `models` 버킷이 보이는지.

### 3-3. 키 두 개 복사

왼쪽 아래 **Project Settings**(톱니) → **API**

| 화면의 이름 | 어디에 넣나 | 성격 |
| --- | --- | --- |
| **Project URL** (`https://xxxx.supabase.co`) | `public/config.js`의 `SUPABASE_URL` | 공개돼도 됨 |
| **anon public** (`eyJ...` 긴 문자열) | `public/config.js`의 `SUPABASE_ANON_KEY` | 공개돼도 됨 |
| **service_role** | **아무 데도 넣지 않습니다** | 절대 공개 금지 |

`public/config.js`를 열어 두 값을 따옴표 안에 붙여넣고 저장합니다. 이 파일은 깃허브에 올라가도 됩니다. anon 키는 브라우저에 실리는 키라서 공개 전제로 만들어진 키이고, 대신 3-2에서 붙인 규칙(RLS)이 "읽기와 쓰기만 되고 지우기는 안 된다"를 지킵니다.

**service_role 키는 표를 통째로 지울 수 있는 키입니다.** 복사도 하지 마세요. 실수로 어디 붙였으면 Project Settings → API → Reset으로 폐기하고 새로 발급합니다. 지우는 게 아니라 폐기입니다.

---

## 4. AI에게 시키기 — 무엇을 쥐어 주나

Claude Code를 `viewer-starter` 폴더에서 엽니다. 한 문장씩 시킵니다.

### 문장 1 — 로컬에서 켜 보기

> 이 폴더 npm install 하고 dev 서버 켜서 브라우저로 열어 줘

`localhost:5173`이 열리고 **샘플 IFC** 버튼을 누르면 건물이 떠야 합니다. 이때는 "로컬 모드"라 저장이 안 됩니다. 뷰어가 뜨는 것만 확인합니다.

### 문장 2 — 수파베이스 붙이기

키를 `config.js`에 넣은 다음:

> config.js에 수파베이스 키 넣었어. 새로고침해서 "수파베이스 연결됨"으로 바뀌는지 보고, 샘플 IFC를 올려서 올라온 모델 목록에 뜨는지 확인해 줘

AI가 화면을 못 보면 여러분이 봅니다. 왼쪽 위 글씨가 **수파베이스 연결됨**으로 바뀌고, 샘플 IFC를 누르면 "올렸어요" 뒤에 목록에 뜹니다. **수파베이스 Table Editor의 `models` 표에 행이 하나 생겼는지** 직접 열어 봅니다. 그게 확인입니다.

### 문장 3 — 깃허브에 올리고 배포하기

> 이 폴더 깃허브에 새 저장소로 올리고 Vercel로 배포해 줘. .env 같은 비밀 파일은 올리지 마

AI가 `git init`, `git add`, `git commit`, `git push`를 치고 `vercel` 명령으로 배포합니다. **직접 치지 않습니다. 지나가는 걸 읽습니다.** 모르는 명령이 지나가면 "그건 뭐야"라고 물어보세요.

주소가 나오면 **옆 사람 폰으로 엽니다.** 열리면 된 겁니다. "Deployed"라는 글자는 확인이 아닙니다.

### 문장 4 — (막혔을 때) 무엇을 쥐어 주나

AI가 헤매면 이 셋을 줍니다. 이 이상은 필요 없습니다.

1. 에러 메시지 **전체** 복사 (일부 말고)
2. 지금 어느 폴더에서 어느 명령을 쳤는지
3. 수파베이스 **Project URL** (키는 config.js에 이미 있으니 다시 줄 필요 없음)

**절대 주지 않는 것:** service_role 키, 수파베이스 데이터베이스 비밀번호, 깃허브 비밀번호.

---

## 5. 서로 코멘트하기

1. 옆 사람 주소를 폰이나 브라우저로 엽니다.
2. 올라온 모델을 누릅니다.
3. **📍 객체 찍기** → 모델의 벽이나 슬래브를 클릭 → 그 객체가 주황으로 켜지고 이름이 뜹니다.
4. 이름과 코멘트를 쓰고 **남기기**.
5. 옆 사람 화면에 번호 마커가 뜨고, 코멘트 목록에 여러분 글이 보입니다. (새로고침 없이 뜹니다.)

취소는 **Esc**. 번호를 누르면 그 객체로 카메라가 갑니다.

---

## 6. 확인표 — "됐습니다"는 확인이 아닙니다

| 단계 | 믿으면 안 되는 것 | 이걸로 확인 |
| --- | --- | --- |
| SQL | "Success" | Table Editor에 표 두 개, Storage에 버킷 하나 |
| 키 | "연결됨" 글씨 | 샘플을 올렸을 때 `models` 표에 행이 생기는지 |
| 깃 | "push 완료" | 깃허브 페이지에 파일이 보이는지 |
| 배포 | "Deployed" | 옆 사람 폰에서 열리는지 |
| 코멘트 | "남겼어요" | `comments` 표에 행이 생기고, 남의 화면에 마커가 뜨는지 |

---

## 7. 안 되면

| 막힌 곳 | 이렇게 |
| --- | --- |
| 수파베이스 가입이 안 됨 | 강사가 주는 URL과 anon 키를 config.js에 넣습니다. 표 하나를 다 같이 씁니다. 계정은 집에서 만듭니다. |
| 깃허브·Vercel 계정이 없음 | 3주 방식(폴더 복사)으로 같은 주소까지 갑니다. 깃은 강사 화면으로 봅니다. |
| 시간이 없음 | 마무리는 하고 갑니다. 배포와 코멘트는 집에서, 주소를 보내는 것이 숙제입니다. |
| DWG 파일이 있음 | 브라우저는 DWG를 못 엽니다. 3주에 쓴 ODA File Converter로 DXF로 바꿔서 올립니다. |
